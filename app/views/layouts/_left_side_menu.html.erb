

</script>


<div class="sidebar-header">
  <h4>SimpleNoteApp</h4>
</div>
<% User.find(current_user.id).notes.each do |note| %>
  <div class="success">
    <p>
      <strong><%= link_to note.title ,note_comments_path(:note_id => note.id), :remote => true %></strong>
      Some text...</p>
  </div>
<% end %>
